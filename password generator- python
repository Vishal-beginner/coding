# standard input example
'''
a = int(input())                        sample code for getting one integer
b, c = map(int, input().split())        sample code for getting two integers
d = float(input())                      sample code for getting one float
e, f, g = map(float, input().split())   sample code for getting three floats
h = input()                             sample code for getting one character
'''

# standard output example
'''
a, b = 6, 3
c, d, e = 1.0, 2.5, 3.4
f = "ABC"
print(a)                                sample code for printing one integer
print(b, end = " ")                     sample code for printing integer variable and space
print(c, d, e)                          sample code for printing three floats
print(f)                                sample code for printing one char
'''

#import sys


'''
	open function below opens input.txt file in read only mode, and afterward,
	the program will read from input.txt file instead of standard(keyboard) input.
	To test your program, you may save input data in input.txt file,
	and use open function to read from the file when using input function.
	You may remove the comment symbols(#) in the below statement and use it.
	But before submission, you must remove the open function or rewrite comment symbols(#).
'''
#sys.stdin = open("input.txt", "r")

T = 10
'''
    If you remove the statement below, your program's output may not be recorded 
    when your program is aborted due to the time limit. 
'''
for test_case in range(1, T + 1):
    # ///////////////////////////////////////////////////////////////////////////////////
    '''
 
        Please, implement your algorithm from this section.
 
    '''
    t = input()
    arr = [int(i) for i in input().split()]
    i=0
    j=0   
    while arr[(i+7)%8] > 0:
        arr[i%8] -= (j%5 +1)
        i += 1
        j += 1
    ans = arr[i%8:]+arr[:i%8]
    ans[7] = 0
    output = "#"+t
    for i in ans:
        output += " "
        output += str(i)
    print(output)
